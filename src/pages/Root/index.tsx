import React from 'react';
import styles from './index.module.less';

const Root: React.FC = () => {
  return (
    <div className={styles['root-container']}>
      Root
    </div>
  );
}

export default Root;
